import { EmailCampaignRepository } from './email-campaign.repository';

describe('EmailCampaignRepository', () => {
  it('should be defined', () => {
    expect(new EmailCampaignRepository()).toBeDefined();
  });
});
